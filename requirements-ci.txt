# requirements-ci.txt
# Lightweight deps for GitHub Actions CI — CPU only, no GPU needed.
# Looser pins to avoid resolver conflicts on ubuntu-latest.

# Core ML
tensorflow-cpu==2.15.1
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
scipy>=1.10,<2.0

# Distributed processing
pyspark==3.5.1
pyarrow>=14.0,<17.0

# Cloud + mocking
boto3>=1.34,<2.0
botocore>=1.34,<2.0
moto[s3]>=5.0,<6.0

# Experiment tracking
mlflow>=2.10,<3.0

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# Utilities
python-dotenv>=1.0,<2.0