# ─────────────────────────────────────────────────────────────────────────────
# requirements-ci.txt
# Lightweight deps for GitHub Actions CI — no GPU, no Docker, fast installs.
# For full local dev, use requirements.txt instead.
# ─────────────────────────────────────────────────────────────────────────────

# Core ML
tensorflow-cpu==2.15.0          # CPU-only build — no CUDA needed on GitHub Actions
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1             # Metrics helpers used in evaluate.py
scipy==1.12.0                   # PSI computation in drift_monitor.py

# Distributed processing
pyspark==3.5.1
pyarrow==15.0.0                 # Parquet read/write

# Cloud
boto3==1.34.34
botocore==1.34.34
moto[s3]==5.0.3                 # AWS mock for unit tests — no real AWS calls in CI

# Experiment tracking
mlflow==2.18.0

# Testing
pytest==8.1.1
pytest-cov==5.0.0

# Utilities
python-dotenv==1.0.1
