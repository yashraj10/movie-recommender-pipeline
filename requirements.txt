# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt  —  Movie Recommender Pipeline
# Python 3.11+  |  For GPU training use Google Colab (see notebooks/)
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core ML ──────────────────────────────────────────────────────────────────
tensorflow==2.15.0              # NCF + MF model training (GPU via Colab)
numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1
scipy==1.12.0

# ── Distributed processing ───────────────────────────────────────────────────
pyspark==3.5.1                  # Feature engineering on 32M ratings
pyarrow==15.0.0                 # Parquet I/O
fastparquet==2024.2.0           # Alternative Parquet engine

# ── Cloud ─────────────────────────────────────────────────────────────────────
boto3==1.34.34                  # AWS S3 data lake
botocore==1.34.34
s3fs==2024.2.0                  # S3 filesystem adapter for pandas/pyarrow

# ── Experiment tracking ───────────────────────────────────────────────────────
mlflow==2.18.0                  # Experiment tracking + model registry

# ── Pipeline orchestration ────────────────────────────────────────────────────
apache-airflow==2.8.1           # DAG orchestration (also via Docker Compose)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-cov==5.0.0
moto[s3]==5.0.3                 # AWS mock for unit/integration tests

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1            # .env loading
tqdm==4.66.2                    # Progress bars during training
matplotlib==3.8.3               # Plotting (EDA + model comparison charts)
seaborn==0.13.2                 # EDA visualisations
jupyterlab==4.1.3               # Local notebook support

# ── Java note ─────────────────────────────────────────────────────────────────
# PySpark requires Java 11 or 17. Install separately:
#   sudo apt-get install -y openjdk-17-jdk   (Linux / Codespaces)
#   brew install openjdk@17                   (macOS)
